{
  "component": "SettingsTemplate",
  "description": "Settings layout with left nav + content area. Responsive: stacks on mobile.",
  "props": {
    "title": { "type": "string", "required": false },
    "navItems": {
      "type": "SettingsNavItem[]",
      "required": true,
      "shape": { "label": "string", "href": "string", "active": "boolean?" }
    },
    "content": {
      "type": "ComponentSlot",
      "required": false,
      "description": "Settings panel slot. Pass ComponentConfig JSON for data-driven rendering OR a custom ReactNode for full control.",
      "example_json": { "component": "ProfileForm", "props": {} },
      "example_jsx": "<ProfileForm user={user} />"
    },
    "className": { "type": "string", "required": false }
  },
  "slot_types": {
    "ComponentSlot": "ComponentConfig JSON (data-driven, registry lookup) OR ReactNode (custom JSX, rendered directly). JSON takes priority."
  },
  "note": "children prop also accepted as backward-compat alias for content."
}