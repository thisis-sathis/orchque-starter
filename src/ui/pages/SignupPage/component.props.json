{
  "component": "SignupPage",
  "description": "Signup/register page. AuthTemplate + name/email/password FormFields + submit Button.",
  "props": {
    "logoText": { "type": "string", "required": false },
    "logoSrc": { "type": "string", "required": false },
    "onSubmit": { "type": "function", "required": true, "description": "(name: string, email: string, password: string) => void" },
    "isLoading": { "type": "boolean", "required": false },
    "errorMessage": { "type": "string", "required": false },
    "loginHref": { "type": "string", "required": false },
    "className": { "type": "string", "required": false }
  }
}
