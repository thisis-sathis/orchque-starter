{
  "component": "SupportPage",
  "description": "Product dashboard support page. DashboardTemplate with PageHeader, new ticket form, and optional ticket history list.",
  "props": {
    "layout": {
      "type": "Omit<DashboardTemplateProps, 'children'>",
      "required": true,
      "description": "DashboardTemplate layout config — sidebar, nav, user info"
    },
    "heading": { "type": "string", "required": false, "description": "Page heading. Default: 'Support'" },
    "subheading": { "type": "string", "required": false, "description": "Page subheading description" },
    "tickets": {
      "type": "SupportTicket[]",
      "required": false,
      "description": "Existing tickets to display below the form. Empty array hides ticket list section.",
      "shape": {
        "id": "string",
        "subject": "string",
        "status": "string",
        "created_at": "string"
      }
    }
  }
}
