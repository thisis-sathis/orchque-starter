{
  "component": "Sheet",
  "description": "Side panel overlay that slides in from any edge. Use for nav drawers, filters, and detail panels.",
  "props": {
    "content": {
      "type": "ComponentSlot",
      "required": false,
      "description": "Body slot. Pass ComponentConfig JSON for data-driven rendering OR a custom ReactNode for full control.",
      "example_json": { "component": "NavMenu", "props": { "items": [] } },
      "example_jsx": "<MyNavMenu />"
    },
    "title": { "type": "string", "required": false },
    "description": { "type": "string", "required": false },
    "trigger": {
      "type": "TriggerSlot",
      "required": false,
      "description": "Trigger slot. Pass TriggerConfig JSON for a data-driven Button OR a custom ReactNode.",
      "example_json": { "label": "Open menu", "variant": "ghost", "icon": "Menu" },
      "example_jsx": "<HamburgerButton />"
    },
    "open": { "type": "boolean", "required": false },
    "onOpenChange": { "type": "(open: boolean) => void", "required": false },
    "side": { "type": "\"left\" | \"right\" | \"top\" | \"bottom\"", "required": false, "default": "\"right\"" }
  },
  "slot_types": {
    "ComponentSlot": "ComponentConfig JSON (data-driven, registry lookup) OR ReactNode (custom JSX, rendered directly). JSON takes priority.",
    "TriggerSlot": "TriggerConfig JSON (renders as Button) OR ReactNode (custom JSX, used directly as trigger). JSON takes priority."
  },
  "note": "children prop also accepted as backward-compat alias for content."
}