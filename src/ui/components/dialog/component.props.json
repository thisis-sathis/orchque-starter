{
  "component": "Dialog",
  "description": "Modal overlay with accessible focus trap. Use for forms, confirmation prompts, and detail views.",
  "props": {
    "content": {
      "type": "ComponentSlot",
      "required": false,
      "description": "Body slot. Pass ComponentConfig JSON for data-driven rendering OR a custom ReactNode (custom JSX) for full control.",
      "example_json": { "component": "MyForm", "props": { "onSubmit": "handleSubmit" } },
      "example_jsx": "<MyCustomForm onSubmit={handleSubmit} />"
    },
    "title": { "type": "string", "required": false, "example": "\"Edit profile\"" },
    "description": { "type": "string", "required": false },
    "trigger": {
      "type": "TriggerSlot",
      "required": false,
      "description": "Trigger slot. Pass TriggerConfig JSON for a data-driven Button OR a custom ReactNode.",
      "example_json": { "label": "Open", "variant": "secondary", "icon": "Settings" },
      "example_jsx": "<MyIconButton />"
    },
    "open": { "type": "boolean", "required": false },
    "onOpenChange": { "type": "(open: boolean) => void", "required": false },
    "size": { "type": "\"sm\" | \"md\" | \"lg\" | \"xl\"", "required": false, "default": "\"md\"" },
    "className": { "type": "string", "required": false }
  },
  "slot_types": {
    "ComponentSlot": "ComponentConfig JSON (data-driven, registry lookup) OR ReactNode (custom JSX, rendered directly). JSON takes priority.",
    "TriggerSlot": "TriggerConfig JSON (renders as Button) OR ReactNode (custom JSX, used directly as trigger). JSON takes priority."
  },
  "note": "children prop also accepted as backward-compat alias for content."
}