{
  "component": "ScrollArea",
  "description": "Custom scrollable container with styled overlay scrollbars.",
  "props": {
    "content": {
      "type": "ComponentSlot",
      "required": false,
      "description": "Body slot. Pass ComponentConfig JSON for data-driven rendering OR a custom ReactNode for full control.",
      "example_json": { "component": "NotificationList", "props": { "items": [] } },
      "example_jsx": "<MyLongList items={items} />"
    },
    "height": { "type": "string", "required": false, "default": "\"300px\"", "example": "\"400px\"" },
    "className": { "type": "string", "required": false }
  },
  "slot_types": {
    "ComponentSlot": "ComponentConfig JSON (data-driven, registry lookup) OR ReactNode (custom JSX, rendered directly). JSON takes priority."
  },
  "note": "children prop also accepted as backward-compat alias for content."
}